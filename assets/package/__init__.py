# -*- coding: utf-8 -*-
"""Sisyphe.io game package.

Decomposed from the original monolithic app.py into small, cohesive modules.
The same package is importable as ``assets.package.*`` (from the root ``app.py``)
and as ``package.*`` (from ``assets/editor.py``, whose folder is on sys.path),
so every intra-package import is relative.
"""
